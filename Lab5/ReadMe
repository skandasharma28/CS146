# Used inorder traversal to get the BST in an ArrayList in a sorted way, in a helper class
# Then used the actual method listed on the Lab to check if the array is actually sorted. 
# Returned true if the array is sorted - BST is valid
# False otherwise
